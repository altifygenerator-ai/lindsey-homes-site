# Lindsey Homes hero + Reserve gallery refinement

Apply this package over the current Lindsey Homes project root and replace the matching files.

Changes in this pass:
- Restores the homepage hero copy to the normal centered site shell so it no longer starts at the edge of the viewport.
- Narrows and slightly reduces the hero headline for a more controlled luxury editorial composition.
- Keeps the hero full-bleed photography and automatic rotation.
- Replaces the bottom-right carousel control cluster with one quiet arrow on each side of the hero, matching an architectural photo-viewer pattern.
- Manual navigation resets the auto-rotation timer so a slide does not change immediately after a visitor clicks.
- Keeps the hero image set focused on Dallas, The Reserve, luxury outdoor living, and Dallas residential reference imagery.
- Simplifies The Reserve gallery to one large 16:9 image at a time with side arrows and a minimal caption/count overlay.
- Removes the extra footer and progress-track clutter from The Reserve gallery.
- Includes mobile and reduced-motion behavior.

Files replaced:
- app/page.tsx
- app/gallery.css
- components/HeroGallery.tsx
- components/ResidenceGallery.tsx
